# GUI-Agent 协作规范与工程方法论（项目级 Agent 配置）

> 本文件沉淀本项目验证过的工作方法论与协作流程，对在本仓库工作的所有 Agent 生效。
> 全局规则（用户背景、CodeGraph 用法、命名架构规范）见用户级 AGENTS.md，不在此重复。

---

## 一、核心方法论：螺旋上升回路

面对任何技术问题，不按「数据先行 vs 设计先行」二分，按所处相位行动：

```
未知 → 设计先行（基于经验的假设）→ 只读验证 → 拿到数据 → 数据先行 →
遇到新未知 → 又回到设计先行 → 循环往复，螺旋上升
```

**每一圈的三个必备要素（缺一不开圈）：**

1. **未知定义**：这一圈要回答的问题是什么，一句话说清。
2. **判据（开圈前写死）**：拿到什么数据**推翻**假设、拿到什么数据**确认**假设，
   两种结果各自导向什么行动。没有判据的探测就是「看看」，禁止做。
3. **出圈产物**：可复查的证据（日志、报告、截图、测试），不是口头结论。

**圈的步长纪律**：便宜、只读、能快速判伪的圈（探测、smoke）高频先行；
贵的圈（大实现、真机任务）等最危险的假设被钉死再启动。
不确定性越大，圈走得越小越快。

**失败模式警惕**：螺旋上升和原地打转外观很像——没有判据、没有产物、
没有推翻过任何假设的循环，就是打转，立即停下来重述未知。

**已沉淀的实例**（新 Agent 可查阅的判例）：

- 坐标尺度问题：未知（glm 坐标约定）→ 只读探测（判据：y>999 即非 0–999）→
  数据（像素坐标，实测差 9px）→ 修复（coordinate_scale 配置）→ 真机验证 →
  新未知（服务端缩放/跨分辨率稳定性）→ 下一圈（红米探测 + 自标定机制）。
- 该问题的终态设计体现了方法论的极致：**把螺旋回路本身产品化**——
  检测未知（漂移信号）→ 形成假设（候选变换）→ 验证（真值比对）→ 采纳，
  让系统自己跑圈，人只在标定失败时介入。

## 二、多 Agent 协同开发流程（P4 验证过的 pipeline）

1. **主 Agent**：全局规划、任务拆解、写死子任务规格与边界、质量门禁、真机/实环境验证、最终放行。
2. **实现子 Agent**（Backend Architect 等）：规格必须包含——要读的文件清单、
   允许创建/修改的文件清单（除此之外一律不碰）、复用哪个现有 seam、测试要求、
   「遇冲突停下来报告，禁止即兴扩大设计」。
3. **Code Reviewer 独立评审**：按安全（fail-closed/泄密面）→ 正确性 → 克制性顺序，
   每条发现给 file:line 证据，结论 APPROVE / APPROVE-WITH-FIXES / REJECT。
4. **回炉返修**：评审发现通过 SendMessage 回到原实现者修复，保持上下文连续。
5. **QA 验收（Reality Checker）**：默认 NEEDS WORK，逐项用可复现证据核验
   （亲自重跑测试、核对产物文件、扫描密钥泄露、目检截图），不接受「声称完成」。
6. **文档（Technical Writer）**：只写已验证的事实，已知坑（如 IME 组合态）诚实记录。

**反过度设计红线**：单一职责优先、能复用不新建、无依据的抽象不做、
speculative feature 不做。子 Agent 交付物超出规格清单即视为缺陷。

## 三、工程纪律（本项目已确立，新代码必须遵守）

- **证据先行**：改任何行为前，先用只读探测拿证据（坐标尺度、解析器缺陷都是这么发现的）。
- **Fail-closed**：模型输出一律不可信；越界拒绝，不 clamp；策略拒绝发生在任何设备输入之前。
- **密钥卫生**：API Key 只在进程内存，绝不打印、不写日志/报告/审计；报错只报变量名不报错值。
- **测试**：离线测试用假件注入（factory seam），不碰真机/真模型/网络；
  全量 `python -m pytest -q` 必须保持绿；真机验证必须留下可复查产物
  （report.json + audit.jsonl + screenshots）。
- **审计**：设备输入事件的审计 payload 只记 argument_keys 不记值。
- **提交**：commit/push 必须用户明确发话；语义化提交信息；提交前核对
  密钥与产物未混入暂存区。

## 四、项目速查

- **CodeGraph**：本项目已索引（`.codegraph/`），代码理解一律先用 `codegraph_explore`，
  不要 Grep 二次验证它已返回的源码。
- **环境**：Windows + Git Bash；Python 用 Anaconda（`python`，3.13）；
  adb 在 `C:/Users/lwj/AppData/Local/Android/Sdk/platform-tools/adb.exe`（不在 PATH）。
- **设备**：默认模拟器 `emulator-5554`；红米真机接入后先跑只读探测包。
- **模型配置**：`.env`（git-ignored）提供 `MAI_UI_BASE_URL`/`MAI_UI_MODEL_NAME`/`BIGMODEL_API_KEY`；
  当前生产模型为智谱 BigModel glm 系（**像素坐标约定**，`coordinate_scale="pixels"`）。
- **入口**：
  - P3 评测：`python -m gui_agent.evaluation.android --serial ... `（四安全场景+报告）
  - 通用任务：`python -m gui_agent.autonomous.android --serial ... "<指令>"`（P4，真机已验证）
- **阶段文档**：`docs/`（P3 评测指南、P4 规划与用户指南、smoke 说明）。
